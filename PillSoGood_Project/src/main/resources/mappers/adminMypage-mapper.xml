<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="adminMypageMapper">

	<resultMap id="memberResultSet" type="member">
		<result column="MEMBER_NO" property="memberNo" />
		<result column="MEMBER_ID" property="memberId" />
		<result column="MEMBER_NAME" property="memberName" />
		<result column="EMAIL" property="email" />
		<result column="MEMBER_ENROLL_DATE" property="memberEnrollDate" />
		<result column="PHONE" property="phone" />
		<result column="addressZip" property="address1" />
		<result column="ADDRESS_1" property="address1" />
		<result column="ADDRESS_2" property="address2" />
		<result column="GENDER" property="gender" />
		<result column="MEMBER_BIRTH_YEAR" property="memberBirthYear" />
		<result column="HEIGHT" property="height" />
		<result column="WEIGHT" property="weight" />
		<result column="MEMBER_STATUS" property="memberStatus" />
	</resultMap>

	
	<select id="selectListCount" resultType="_int">
		SELECT COUNT(*)
		FROM MEMBER
		WHERE MEMBER_STATUS = 'Y'
	</select>
	
	
	<select id="selectList" resultMap="memberResultSet">
		SELECT MEMBER_NO, MEMBER_ID, MEMBER_NAME,
		       EMAIL, MEMBER_ENROLL_DATE, MEMBER_STATUS
		FROM MEMBER
	</select>
	
	<select id="selectMember" resultMap="memberResultSet" parameterType="_int">
		SELECT MEMBER_NO, MEMBER_ID, MEMBER_NAME, PHONE,
		       EMAIL, GENDER, ADDRESS_ZIP, ADDRESS_1, ADDRESS_2,
		       MEMBER_BIRTH_YEAR, HEIGHT, WEIGHT, MEMBER_ENROLL_DATE,
		       MEMBER_STATUS
		FROM MEMBER
		WHERE MEMBER_NO = #{memberNo}
	
	</select>
	
</mapper>